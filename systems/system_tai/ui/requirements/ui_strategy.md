Loại truy vấn Mục tiêu UI Bố cục tổng thể Khu vực nhập truy vấn Khu vực kết quả Khu vực video / temporal Khu vực evidence Khu vực answer list Thao tác chính Phím tắt đề xuất Trạng thái cần lưu Tối ưu Frontend Rủi ro UX
Shared Layout Giữ trải nghiệm nhất quán giữa ba task Header + Task Tabs + Query Panel + Main Workspace + Answer Drawer + Status Bar Task selector; dataset/batch; query ID; chế độ Interactive/Automatic Dùng component chung nhưng nội dung thay đổi theo task Video player và timestamp dùng chung Evidence panel dạng tab: Visual, OCR, ASR, Object, Metadata Answer drawer bên phải hoặc phía dưới Search; reset; back; open candidate; add answer; remove; reorder; validate; export Enter: search; Esc: đóng panel; Ctrl+Enter: thêm answer; Ctrl+S: lưu state query; filters; selected candidates; player timestamp; answer list; search history Code splitting theo task; shared state store; API cancellation; loading/error state; cache thumbnail Giao diện quá nhiều panel; người mới khó hiểu; mất trạng thái khi đổi task
Textual KIS Tìm nhanh candidate, mở đúng video và chọn exact frame Query Panel bên trái; Result Grid ở giữa; Player + Filmstrip bên phải; Answer Drawer phía dưới Text query; query variants; modality filters; source filter; search button Result grid nhóm theo video; mỗi card có thumbnail, video_id, actual frame ID, timestamp, score và evidence badges Player mở đúng timestamp; filmstrip before/current/after; timeline; nút search-in-video Visual match; OCR text; ASR transcript; detected objects; metadata; confidence Danh sách video_id + actual frame_id; reorder; duplicate warning; validate Search → inspect grid → open video → refine temporal window → select exact frame → add answer → validate 1–9 mở candidate; A thêm answer; ←/→ đổi frame; Shift+←/→ nhảy xa; V mở video query gốc; query variants; filters; video group; selected frame; answer ranking Virtualized grid; lazy-load thumbnail; prefetch frame lân cận; debounce search; cancel request cũ; thumbnail cache Nhiều candidate gần trùng; người dùng nhầm keyframe order với actual frame ID; filmstrip tải chậm
Q&A Tìm evidence trước rồi mới chọn answer và representative frame Question Form phía trên; Candidate Videos bên trái; Evidence Workspace ở giữa; Answer Panel bên phải Event description; question; temporal relation; optional answer type Candidate video list nhóm theo confidence; hiển thị event-match score và evidence availability Player + evidence interval timeline; anchor marker; before/after/during region Visual frames; OCR; ASR; object; metadata; answer hypotheses; answer-evidence consistency video_id + actual frame_id + canonical answer; confidence; evidence link Retrieve event → open video → localize evidence interval → inspect evidence → choose/normalize answer → add tuple → validate Q focus question; E mở evidence; T mở transcript; O mở OCR; A thêm answer event description; question; answer type; evidence interval; selected frame; answer hypotheses; canonical answer Đồng bộ player với transcript/OCR; highlight evidence span; cache answer hypotheses; controlled form validation Người dùng chọn answer đúng nhưng frame sai; quá tin confidence; answer variants gây duplicate; evidence panel quá tải
TRAKE Hiểu và chỉnh một chuỗi event trong cùng video, đúng thứ tự Event Decomposition trên cùng; Video Ranking bên trái; Multi-event Timeline ở giữa; Event Columns và Chain List bên phải Query gốc; editable E1…En; temporal constraints; nút retrieve all events Video ranking theo event coverage; hiển thị event nào đã tìm thấy hoặc còn thiếu Timeline chung có marker màu cho E1…En; gap display; mở candidate quanh từng event Evidence riêng cho từng event; chain-level consistency; same-video và order validation video_id + frame_id_1…frame_id_n; chain confidence; completeness; duplicate-chain warning Decompose → retrieve từng event → chọn video → xem Top-N chains → sửa frame từng event → verify chain → add answer 1–9 chọn event; J/K đổi event; ←/→ đổi frame; C thêm chain; X loại chain event definitions; candidate per event; selected video; Top-N chains; exact frames; gap constraints Virtualize candidate columns; chỉ render event đang active; prefetch lân cận; worker cho chain visualization; optimistic UI khi sửa frame Candidate explosion; timeline quá dày; nhầm event; chọn frame khác video; chain editor khó dùng trên màn hình nhỏ
