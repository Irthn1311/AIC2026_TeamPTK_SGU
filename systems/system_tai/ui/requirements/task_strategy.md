Loại truy vấn Input chính thức Output chính thức Mục tiêu xử lý Query preparation Candidate retrieval Temporal / local refinement Evidence / answer logic Chiến lược UI Frontend components Backend modules Chiến lược Top-100 Rủi ro chính Metric kiểm chứng
Textual KIS Mô tả đầy đủ một sự kiện bằng văn bản video_id, frame_id Tìm đúng video và một actual frame ID nằm trong semantic moment của sự kiện Chuẩn hóa query; trích những cue thực sự xuất hiện như action, object, person, location, visual attribute, OCR hoặc ASR; tách cue bắt buộc và cue bổ trợ; chỉ sinh số lượng query variant giới hạn; gắn trọng số và lưu query gốc để tránh query drift Chạy CLIP vector search kết hợp text index cho metadata, OCR, ASR và caption; dùng object detection như soft evidence; fusion hoặc rerank kết quả; deduplicate candidate gần trùng; aggregate và xếp hạng candidate theo video Chọn Top candidate video; mở temporal neighborhood thích ứng quanh candidate; search-in-video theo video hoặc shot; dense frame sampling cục bộ; Semantic Moment Localizer chọn các frame hypothesis nằm trong semantic interval; chuyển keyframe order sang actual frame ID qua Unified Frame Mapping Kiểm tra visual match, OCR, ASR, object cue và temporal context; không loại cứng candidate chỉ vì thiếu một modality; candidate cuối phải kèm video_id, timestamp, actual frame ID, evidence và confidence; từ chối candidate không ánh xạ được sang frame hợp lệ Interactive mode: result grid nhóm theo video → mở video đúng timestamp → filmstrip before/current/after → refine trong video → xem evidence → chọn exact frame → thêm vào answer list; Automatic mode: cùng Backend Core tự localize, verify, rank và xuất danh sách Query input; query-variant chips; filter theo modality; virtualized result grid; video group card; synchronized video player; timeline; filmstrip; evidence panel; exact-frame selector; ranked answer-list editor; submission preview Query Compiler; Multimodal Retrieval; Vector Search; Text Search; Rank Fusion/Reranker; Video Aggregator; In-video Search; Temporal Neighborhood; Semantic Moment Localizer; Evidence Verifier; Unified Frame Mapping; Ranked Answer List Constructor; Submission Validator; Official Metric Evaluator; Logging/Execution Trace Top-1 và Top-5 ưu tiên candidate có confidence và evidence mạnh nhất; Top-20 phủ các frame hypothesis đại diện cho vùng bất định của semantic moment; Top-50 và Top-100 bổ sung candidate video cạnh tranh và retrieval branch khác; deduplicate theo cùng video, temporal overlap và semantic similarity; không đưa hàng chục frame liên tiếp hoặc near-duplicate vào danh sách Đúng video nhưng sai frame; query expansion làm lệch ý; Retrieval không đưa video đúng vào Top-K; temporal window quá hẹp; lỗi ánh xạ n, CLIP row và frame_idx; object/OCR/ASR tạo false evidence; Top-100 chứa quá nhiều near-duplicate; output sai format Video Recall@K; frame Recall@K; MRR; semantic-frame accuracy; Temporal IoU; start/end error; R@1; R@5; R@20; R@50; R@100; duplicate rate; end-to-end latency; P95 latency; failure rate; invalid-output rate; time-to-submit; frames inspected
Q&A Mô tả sự kiện kèm câu hỏi video_id, frame_id, answer Tìm đúng video, localize đúng evidence interval, chọn một representative semantic frame hợp lệ và trả lời đúng câu hỏi Tách event description khỏi question; chuẩn hóa câu hỏi; trích anchor event và temporal relation như before, after hoặc during; dùng Answer Type Router hoặc Optional Classifier để gợi ý tool cho action, object, person, count, color, location, OCR text, ASR speech hoặc yes/no; không làm pipeline thất bại nếu loại câu trả lời không rõ Retrieve candidate video chủ yếu từ event description; kết hợp CLIP, metadata, caption, OCR và ASR; dùng object cue như tín hiệu phụ; aggregate theo video và xếp hạng video-first; chỉ đưa Top video vào bước evidence localization Localize anchor event và evidence interval trước; xác định vùng trước, sau hoặc trong anchor tùy câu hỏi; refine clip hoặc frame chứa bằng chứng; chọn một representative semantic frame nằm trong evidence interval để tạo output chính thức; kiểm tra frame qua Unified Frame Mapping Chỉ extract answer sau khi đã có evidence; thu thập visual, OCR, ASR, object và temporal evidence phù hợp answer type; tạo answer hypothesis; chuẩn hóa về canonical form; kiểm tra answer-evidence consistency; trả confidence; reject answer khi evidence không đủ hoặc mâu thuẫn Interactive mode: event description và question tách riêng → candidate video → evidence timeline → transcript/OCR/visual evidence → answer hypotheses → chọn answer tuple; Automatic mode: tự retrieve, localize evidence, extract, normalize, verify và rank Q&A form; event-description field; question field; answer-type badge; candidate video list; evidence timeline; synchronized player; transcript panel; OCR panel; evidence panel; answer-hypothesis selector; confidence display; canonical-answer preview; ranked answer-list editor Event Retriever; Multimodal Retrieval; Video Aggregator; Anchor Localizer; Evidence Interval Localizer; Representative Frame Selector; Evidence Collector; Answer Type Router/Optional Classifier; Answer Extractor; Answer Normalizer; Answer-Evidence Verifier; Unified Frame Mapping; Ranked Answer List Constructor; Submission Validator; Official Metric Evaluator; Logging/Execution Trace Mỗi record phải là một tuple hoàn chỉnh gồm video_id, actual frame_id và canonical answer; Top đầu ưu tiên tuple có evidence mạnh và answer-confidence cao; diversity chủ yếu đến từ candidate video, evidence interval và representative frame; không tạo nhiều record chỉ khác dấu câu, chính tả hoặc cách viết đồng nghĩa của cùng một answer; loại duplicate sau normalization Answer đúng nhưng frame sai vẫn thất bại; localize đúng video nhưng sai evidence interval; hallucination; answer đúng do đoán nhưng evidence sai; Answer Type Router phân loại không chắc chắn; count thay đổi giữa các frame; OCR hoặc ASR sai; canonicalization làm đổi nghĩa; answer rỗng hoặc sai format Answer accuracy; evidence localization accuracy; representative-frame accuracy; grounded answer rate; answer-evidence consistency rate; full-tuple accuracy; R@1; R@5; R@20; R@50; R@100; end-to-end latency; P95 latency; failure rate; invalid-output rate; time-to-submit; evidence spans inspected
TRAKE Chuỗi nhiều sự kiện E1 → E2 → … → En video_id, frame_id_1, …, frame_id_n Tìm đúng một video chứa đủ các event theo đúng thứ tự thời gian và chọn một semantic frame hợp lệ cho từng event Tách query thành E1…En; chuẩn hóa từng event độc lập; trích action, object, participant, location và cue OCR/ASR; xác định hard order constraints và optional temporal relation như before, after, then hoặc during; sinh query variant có kiểm soát riêng cho từng event Retrieve candidate độc lập cho từng event bằng vector search và text search; aggregate candidate theo cùng video; tính event coverage; rank video theo số event được phủ, retrieval score, modality support và temporal consistency; loại hoặc hạ hạng video thiếu quá nhiều event Trong từng candidate video, tạo candidate list theo event; dùng Dynamic Programming hoặc Beam Search có giới hạn beam width và Top-N chain để tìm monotonic event chain; áp dụng gap constraints và sequence coherence; không sinh Cartesian product toàn bộ; dense refinement và Semantic Moment Localizer chọn representative frame cho từng event Verify từng event và toàn chuỗi; kiểm tra tất cả frame thuộc cùng video, đúng thứ tự, không vi phạm temporal constraints và có semantic evidence; đánh giá event-level confidence và chain-level confidence; reject chain thiếu event hoặc có event evidence quá yếu Interactive mode: query chia thành các event column → candidate video ranking → timeline chung → candidate theo từng event → Top-N chains → xem và sửa exact frame từng event → thêm chain vào answer list; Automatic mode: tự decompose, retrieve, align, localize, verify và rank Event decomposition panel; editable event cards; video coverage ranking list; multi-event timeline; event candidate columns; chain viewer; chain editor; gap display; exact-frame selector; chain-confidence display; validation warnings; ranked chain-list editor Query Decomposer; Controlled Query Expansion; Per-event Multimodal Retrieval; Video Coverage Ranker; Same-video Aggregator; Temporal Alignment bằng DP hoặc bounded Beam Search; Gap Constraint Evaluator; Sequence Coherence Scorer; Semantic Moment Localizer; Event Verifier; Chain Verifier; Unified Frame Mapping; Chain Ranker; Ranked Answer List Constructor; Submission Validator; Official Metric Evaluator; Logging/Execution Trace Top-1 và Top-5 ưu tiên chain hoàn chỉnh, đúng video, đủ event và có chain-confidence cao; Top-20 mở rộng các representative frame khác trong cùng semantic moment; Top-50 và Top-100 bổ sung alternative alignment và candidate video cạnh tranh; deduplicate chain theo video, event-frame overlap và semantic similarity; giới hạn số chain trên mỗi video; tuyệt đối không sinh toàn bộ Cartesian product Sai video làm cả tuple thất bại; một event bị Retrieval bỏ sót; candidate explosion; event bị gán sai thứ tự; gap constraint quá chặt hoặc quá lỏng; beam width quá nhỏ bỏ sót chain đúng, quá lớn gây latency; frame của từng event nằm ngoài vùng chấm; chain thiếu event; output sai số lượng frame Video accuracy; video Recall@K; event coverage; per-event Recall@K; event-order accuracy; per-event frame accuracy; semantic-frame accuracy; full-chain accuracy; chain completeness; R@1; R@5; R@20; R@50; R@100; candidate chains explored; beam-search latency; end-to-end latency; P95 latency; failure rate; invalid-output rate; time-to-submit
Lưu ý: confidence, evidence span, Temporal IoU, MRR, latency và các metric trung gian là metric nội bộ phục vụ debug và tối ưu. Output gửi BTC vẫn phải đúng định dạng chính thức của từng task.
Cả ba task dùng chung Unified Frame Mapping, Retrieval Core, Submission Validator, Official Metric Evaluator và Logging; chỉ khác pipeline xử lý theo loại truy vấn.
