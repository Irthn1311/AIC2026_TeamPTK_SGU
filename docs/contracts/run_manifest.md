# RunManifest contract

Owner: Hữu Tri. Mọi artifact directory chứa unique run ID, UTC creation time, exact git commit
(`UNKNOWN` nếu ngoài Git), config path/copy, data version, artifact name, command và lifecycle status.

