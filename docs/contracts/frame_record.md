# FrameRecord contract

Owner: Hữu Tri. `actual_frame_id` là ID raw-video dùng cho submission; tuyệt đối không thay bằng
keyframe order. `source_policy` ghi rõ nguồn và mapping mang dataset/extraction version.

