# Test fixtures

Các fixture ở đây chỉ là metadata nhỏ, không chứa video, ảnh hay feature binary. Frame ID trong
manifest và prediction luôn là `actual_frame_id` của raw video, không phải thứ tự keyframe.

